spring.datasource.url=jdbc:h2:mem:gestpro-test;MODE=MySQL;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=create-drop
spring.flyway.enabled=false
spring.jpa.open-in-view=false
spring.data.redis.repositories.enabled=false

# Segredo descartável criado dentro do processo de teste; nenhum JWT de teste
# permanece válido entre execuções ou é versionado no repositório.
jwt.secret=#{T(java.util.Base64).getEncoder().encodeToString(new byte[32])}
resend.api.key=test-key
spring.mail.username=test@example.com
spring.mail.password=test-password
cloudinary.cloud-name=test
cloudinary.api-key=test
cloudinary.api-secret=test
CLOUDINARY_CLOUD_NAME=test
CLOUDINARY_API_KEY=test
CLOUDINARY_API_SECRET=test

spring.security.oauth2.client.registration.google.client-id=test-client
spring.security.oauth2.client.registration.google.client-secret=test-secret

stripe.api.key=sk_test_placeholder
stripe.webhook.secret=whsec_test_placeholder
stripe.price.basico=price_basico_monthly
stripe.price.pro=price_pro_monthly
stripe.price.premium=price_premium_monthly

app.base-url=http://localhost:8080
app.frontend.url=http://localhost:3000
app.cors.allowed-origins=http://localhost:3000
app.cookie.secure=false
app.cookie.same-site=Lax
gestpro.marketplace.shopee.webhook-url=http://localhost:8080/api/marketplace/webhooks/shopee
# Chave descartável criada pelo SpEL somente dentro do processo de teste.
# Não existe material criptográfico versionado no repositório.
FISCAL_MASTER_KEY=#{T(java.util.Base64).getEncoder().encodeToString(new byte[32])}
